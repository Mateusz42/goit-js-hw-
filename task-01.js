const ListWithId = document.querySelectorAll('#categories');

const numberOfCategories = ListWithId.querySelectorAll(".item");
console.log("Number of categories: " + menuItemsByTagName.length);

console.log(menuItemsByTagName);